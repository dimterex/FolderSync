<UserControl x:Class="FolderSyns.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mvvm:ViewModelLocator.AutoWireViewModel="True"
            xmlns:viewModels="clr-namespace:FolderSyns.ViewModels"
            xmlns:mvvm="http://prismlibrary.com/"
            d:DataContext="{d:DesignInstance viewModels:HistoryViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200">
    <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding FileActions}" IsReadOnly="True">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Время" Binding="{Binding DateTime, Converter={StaticResource HistoryDateTimeConverter}}" />
            <DataGridTextColumn Header="Файл" Binding="{Binding FileName}" />
            <DataGridTextColumn Header="Действие" Binding="{Binding Converter={StaticResource HistoryItemConverter}}" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
