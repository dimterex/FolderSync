<Window x:Class="FolderSyns.MVVM.HistoryUserControl.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:windows="clr-namespace:FolderSyns.Windows"
             xmlns:conderters="clr-namespace:FolderSyns.Conderters"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200">
    <Window.Resources>
        <ResourceDictionary>
            <conderters:HistoryItemConverter x:Key="HistoryItemConverter" x:Name="HistoryItemConverter" />
            <conderters:HistoryDateTimeConverter x:Key="HistoryDateTimeConverter" x:Name="HistoryDateTimeConverter" />
        </ResourceDictionary>
    </Window.Resources>

    <DataGrid AutoGenerateColumns="False" IsReadOnly="True">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Время" Binding="{Binding DateTime, Converter={StaticResource HistoryDateTimeConverter}}" />
            <DataGridTextColumn Header="Файл" Binding="{Binding FileName}" />
            <DataGridTextColumn Header="Действие" Binding="{Binding Converter={StaticResource HistoryItemConverter}}" />
        </DataGrid.Columns>
    </DataGrid>
</Window>
